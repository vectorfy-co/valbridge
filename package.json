{
  "name": "valbridge",
  "private": true,
  "type": "module",
  "description": "Bridge Zod and Pydantic with runtime conversion and code generation",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^20.5.3",
    "husky": "^9.0.0"
  }
}
