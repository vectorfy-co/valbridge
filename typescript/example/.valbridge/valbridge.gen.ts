// Generated by valbridge - DO NOT EDIT
// https://github.com/vectorfy-co/valbridge/docs
import { z } from "zod"

const another_TSConfig = (() => {
      const schema = z.intersection((() => {
      const schema = z.object({}).passthrough();
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { title: "JSON schema for the TypeScript compiler's configuration file" }) : schema;
    })(), z.intersection(z.intersection(z.intersection(z.intersection(z.intersection(z.intersection(z.intersection(z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ compilerOptions: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ allowArbitraryExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })()]).describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })().optional(), allowImportingTsExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })()]).describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })().optional(), allowJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })()]).describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })().optional(), allowSyntheticDefaultImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })()]).describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })().optional(), allowUmdGlobalAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })()]).describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })().optional(), allowUnreachableCode: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })()]).describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })().optional(), allowUnusedLabels: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })()]).describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })().optional(), alwaysStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })()]).describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })().optional(), assumeChangesOnlyAffectDirectDependencies: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })()]).describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })().optional(), baseUrl: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })()]).describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })().optional(), charset: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })()]).describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })().optional(), checkJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })()]).describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })().optional(), composite: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })()]).describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })().optional(), customConditions: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })()]).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })().optional(), declaration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })()]).describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })().optional(), declarationDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })()]).describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })().optional(), declarationMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })()]).describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })().optional(), diagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })()]).describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })().optional(), disableReferencedProjectLoad: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })()]).describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })().optional(), disableSizeLimit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })()]).describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })().optional(), disableSolutionSearching: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })()]).describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })().optional(), disableSourceOfProjectReferenceRedirect: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })()]).describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })().optional(), downlevelIteration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })()]).describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })().optional(), emitBOM: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })()]).describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })().optional(), emitDeclarationOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })()]).describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })().optional(), emitDecoratorMetadata: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })()]).describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })().optional(), erasableSyntaxOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })()]).describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })().optional(), esModuleInterop: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })()]).describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })().optional(), exactOptionalPropertyTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })()]).describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })().optional(), experimentalDecorators: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })()]).describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })().optional(), extendedDiagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })()]).describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })().optional(), fallbackPolling: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "fixedInterval", "priorityInterval", "dynamicPriority", "fixedChunkSize"]).describe("Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later." }) : schema;
    })().optional(), forceConsistentCasingInFileNames: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })()]).describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })().optional(), generateCpuProfile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })()]).describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })().optional(), importHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })()]).describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })().optional(), importsNotUsedAsValues: (() => {
      const schema = z.enum(["remove", "preserve", "error"]).describe("Specify emit/checking behavior for imports that are only used for types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify emit/checking behavior for imports that are only used for types.", default: "remove" }) : schema;
    })().optional(), incremental: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })()]).describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })().optional(), inlineSourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), inlineSources: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), isolatedDeclarations: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })()]).describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })().optional(), isolatedModules: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })()]).describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })().optional(), jsx: (() => {
      const schema = z.enum(["preserve", "react", "react-jsx", "react-jsxdev", "react-native"]).describe("Specify what JSX code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what JSX code is generated." }) : schema;
    })().optional(), jsxFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })()]).describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })().optional(), jsxFragmentFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })()]).describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })().optional(), jsxImportSource: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })()]).describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })().optional(), keyofStringsOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })()]).describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })().optional(), lib: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.intersection(z.string(), z.union([z.enum(["ES5", "ES6", "ES2015", "ES2015.Collection", "ES2015.Core", "ES2015.Generator", "ES2015.Iterable", "ES2015.Promise", "ES2015.Proxy", "ES2015.Reflect", "ES2015.Symbol.WellKnown", "ES2015.Symbol", "ES2016", "ES2016.Array.Include", "ES2017", "ES2017.Intl", "ES2017.Object", "ES2017.SharedMemory", "ES2017.String", "ES2017.TypedArrays", "ES2017.ArrayBuffer", "ES2018", "ES2018.AsyncGenerator", "ES2018.AsyncIterable", "ES2018.Intl", "ES2018.Promise", "ES2018.Regexp", "ES2019", "ES2019.Array", "ES2019.Intl", "ES2019.Object", "ES2019.String", "ES2019.Symbol", "ES2020", "ES2020.BigInt", "ES2020.Promise", "ES2020.String", "ES2020.Symbol.WellKnown", "ESNext", "ESNext.Array", "ESNext.AsyncIterable", "ESNext.BigInt", "ESNext.Collection", "ESNext.Intl", "ESNext.Iterator", "ESNext.Object", "ESNext.Promise", "ESNext.Regexp", "ESNext.String", "ESNext.Symbol", "DOM", "DOM.AsyncIterable", "DOM.Iterable", "ScriptHost", "WebWorker", "WebWorker.AsyncIterable", "WebWorker.ImportScripts", "Webworker.Iterable", "ES7", "ES2021", "ES2020.SharedMemory", "ES2020.Intl", "ES2020.Date", "ES2020.Number", "ES2021.Promise", "ES2021.String", "ES2021.WeakRef", "ESNext.WeakRef", "ES2021.Intl", "ES2022", "ES2022.Array", "ES2022.Error", "ES2022.Intl", "ES2022.Object", "ES2022.String", "ES2022.SharedMemory", "ES2022.RegExp", "ES2023", "ES2023.Array", "ES2024", "ES2024.ArrayBuffer", "ES2024.Collection", "ES2024.Object", "ES2024.Promise", "ES2024.Regexp", "ES2024.SharedMemory", "ES2024.String", "Decorators", "Decorators.Legacy", "ES2017.Date", "ES2023.Collection", "ESNext.Decorators", "ESNext.Disposable", "ESNext.Error", "ESNext.Sharedmemory"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]5|[Ee][Ss]6|[Ee][Ss]7$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2015(\\.([Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Cc][Oo][Rr][Ee]|[Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Pp][Rr][Oo][Xx][Yy]|[Rr][Ee][Ff][Ll][Ee][Cc][Tt]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2016(\\.[Aa][Rr][Rr][Aa][Yy]\\.[Ii][Nn][Cc][Ll][Uu][Dd][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2017(\\.([Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Tt][Yy][Pp][Ee][Dd][Aa][Rr][Rr][Aa][Yy][Ss]|[Dd][Aa][Tt][Ee]|[Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2018(\\.([Aa][Ss][Yy][Nn][Cc][Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2019(\\.([Aa][Rr][Rr][Aa][Yy]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2020(\\.([Bb][Ii][Gg][Ii][Nn][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ii][Nn][Tt][Ll]|[Dd][Aa][Tt][Ee]|[Nn][Uu][Mm][Bb][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2021(\\.([Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2022(\\.([Aa][Rr][Rr][Aa][Yy]|[Ee][Rr][Rr][Oo][Rr]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2023(\\.([Aa][Rr][Rr][Aa][Yy]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2024(\\.([Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss][Nn][Ee][Xx][Tt](\\.([Aa][Rr][Rr][Aa][Yy]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Bb][Ii][Gg][Ii][Nn][Tt]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]|[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss]|[Dd][Ii][Ss][Pp][Oo][Ss][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Oo][Mm](\\.([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ss][Cc][Rr][Ii][Pp][Tt][Hh][Oo][Ss][Tt]$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ww][Ee][Bb][Ww][Oo][Rr][Kk][Ee][Rr](\\.([Ii][Mm][Pp][Oo][Rr][Tt][Ss][Cc][Rr][Ii][Pp][Tt][Ss]|([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss](\\.([Ll][Ee][Gg][Aa][Cc][Yy]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })]))).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })()]).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })().optional(), libReplacement: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })()]).describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })().optional(), listEmittedFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })()]).describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })().optional(), listFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })()]).describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })().optional(), listFilesOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })()]).describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })().optional(), mapRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })()]).describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })().optional(), maxNodeModuleJsDepth: (() => {
      const schema = z.union([(() => {
      const schema = z.number().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })()]).describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })().optional(), module: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })()]).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), z.union([z.enum(["commonjs", "amd", "system", "umd", "es6", "es2015", "es2020", "esnext", "none", "es2022", "node16", "node18", "node20", "nodenext", "preserve"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Cc][Oo][Mm][Mm][Oo][Nn][Jj][Ss]|[AaUu][Mm][Dd]|[Ss][Yy][Ss][Tt][Ee][Mm]|[Ee][Ss]([356]|20(1[567]|2[02])|[Nn][Ee][Xx][Tt])|[Nn][Oo][dD][Ee](1[68]|20)|[Nn][Oo][Dd][Ee][Nn][Ee][Xx][Tt]|[Nn][Oo][Nn][Ee]|[Pp][Rr][Ee][Ss][Ee][Rr][Vv][Ee])$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })().optional(), moduleDetection: (() => {
      const schema = z.enum(["auto", "legacy", "force"]).describe("Control what method is used to detect module-format JS files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Control what method is used to detect module-format JS files." }) : schema;
    })().optional(), moduleResolution: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })()]).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), z.union([z.enum(["classic", "node", "node10", "node16", "nodenext", "bundler"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(([Nn]ode)|([Nn]ode1[06])|([Nn]ode[Nn]ext)|([Cc]lassic)|([Bb]undler))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })().optional(), moduleSuffixes: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })(), (() => {
      const schema = z.null().describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })()]).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })().optional(), newLine: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })()]).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), z.union([z.enum(["crlf", "lf"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(CRLF|LF|crlf|lf)$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })().optional(), noCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })()]).describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })().optional(), noEmit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })()]).describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })().optional(), noEmitHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })()]).describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })().optional(), noEmitOnError: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })()]).describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })().optional(), noErrorTruncation: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })()]).describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })().optional(), noFallthroughCasesInSwitch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })()]).describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })().optional(), noImplicitAny: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })()]).describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })().optional(), noImplicitOverride: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })()]).describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })().optional(), noImplicitReturns: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })()]).describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })().optional(), noImplicitThis: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })()]).describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })().optional(), noImplicitUseStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })().optional(), noLib: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })()]).describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })().optional(), noPropertyAccessFromIndexSignature: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })()]).describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })().optional(), noResolve: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })()]).describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })().optional(), noStrictGenericChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })()]).describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })().optional(), noUncheckedIndexedAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })()]).describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })().optional(), noUncheckedSideEffectImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })()]).describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })().optional(), noUnusedLocals: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })()]).describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })().optional(), noUnusedParameters: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })()]).describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })().optional(), out: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })()]).describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })().optional(), outDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })()]).describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })().optional(), outFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })()]).describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })().optional(), paths: (() => {
      const schema = z.union([(() => {
      const schema = z.record(z.string(), z.union([z.array((() => {
      const schema = z.string().describe("Path mapping to be computed relative to baseUrl option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Path mapping to be computed relative to baseUrl option." }) : schema;
    })()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }), z.null()])).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })()]).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })().optional(), plugins: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.object({ name: (() => {
      const schema = z.string().describe("Plugin name.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Plugin name." }) : schema;
    })().optional() }).passthrough()).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })()]).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })().optional(), preserveConstEnums: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })()]).describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })().optional(), preserveSymlinks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })()]).describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })().optional(), preserveValueImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })()]).describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })().optional(), preserveWatchOutput: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })()]).describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })().optional(), pretty: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })()]).describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })().optional(), reactNamespace: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })()]).describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })().optional(), removeComments: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })()]).describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })().optional(), resolveJsonModule: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })()]).describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })().optional(), resolvePackageJsonExports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })().optional(), resolvePackageJsonImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })().optional(), rewriteRelativeImportExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })()]).describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })().optional(), rootDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })()]).describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })().optional(), rootDirs: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })()]).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })().optional(), skipDefaultLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })()]).describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })().optional(), skipLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })()]).describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })().optional(), sourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })().optional(), sourceRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })()]).describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })().optional(), strict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })()]).describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })().optional(), strictBindCallApply: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })()]).describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })().optional(), strictBuiltinIteratorReturn: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })()]).describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })().optional(), strictFunctionTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })()]).describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })().optional(), strictNullChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })()]).describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })().optional(), strictPropertyInitialization: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })()]).describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })().optional(), stripInternal: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })()]).describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })().optional(), suppressExcessPropertyErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })()]).describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })().optional(), suppressImplicitAnyIndexErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })()]).describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })().optional(), target: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })()]).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), z.union([z.enum(["es3", "es5", "es6", "es2015", "es2016", "es2017", "es2018", "es2019", "es2020", "es2021", "es2022", "es2023", "es2024", "es2025", "esnext"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Ee][Ss]([356]|(20(1[56789]|2[012345]))|[Nn][Ee][Xx][Tt]))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })().optional(), traceResolution: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })()]).describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })().optional(), tsBuildInfoFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })()]).describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })().optional(), typeRoots: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })()]).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })().optional(), types: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })()]).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })().optional(), useDefineForClassFields: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })()]).describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })().optional(), useUnknownInCatchVariables: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })()]).describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })().optional(), verbatimModuleSyntax: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })()]).describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })().optional(), watch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })()]).describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })().optional(), watchDirectory: (() => {
      const schema = z.enum(["useFsEvents", "fixedPollingInterval", "dynamicPriorityPolling", "fixedChunkSizePolling"]).describe("Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional(), watchFile: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "useFsEvents", "useFsEventsOnParentDirectory", "fixedChunkSizePolling"]).describe("Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional() }).passthrough().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })()]).describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ compileOnSave: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable Compile-on-Save for this project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable Compile-on-Save for this project." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable Compile-on-Save for this project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable Compile-on-Save for this project." }) : schema;
    })()]).describe("Enable Compile-on-Save for this project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable Compile-on-Save for this project." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ typeAcquisition: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ enable: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable auto type acquisition");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable auto type acquisition", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable auto type acquisition");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable auto type acquisition", default: false }) : schema;
    })()]).describe("Enable auto type acquisition");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable auto type acquisition", default: false }) : schema;
    })().optional(), exclude: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })()]).describe("Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })().optional(), include: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })()]).describe("Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })().optional() }).passthrough().describe("Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later." }) : schema;
    })()]).describe("Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ allOf: z.any().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ watchOptions: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ excludeDirectories: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Remove a list of directories from the watch process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of directories from the watch process." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove a list of directories from the watch process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of directories from the watch process." }) : schema;
    })()]).describe("Remove a list of directories from the watch process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of directories from the watch process." }) : schema;
    })().optional(), excludeFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Remove a list of files from the watch mode's processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of files from the watch mode's processing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove a list of files from the watch mode's processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of files from the watch mode's processing." }) : schema;
    })()]).describe("Remove a list of files from the watch mode's processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of files from the watch mode's processing." }) : schema;
    })().optional(), fallbackPolling: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify what approach the watcher should use if the system runs out of native file watchers.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what approach the watcher should use if the system runs out of native file watchers." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify what approach the watcher should use if the system runs out of native file watchers.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what approach the watcher should use if the system runs out of native file watchers." }) : schema;
    })()]).describe("Specify what approach the watcher should use if the system runs out of native file watchers.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what approach the watcher should use if the system runs out of native file watchers." }) : schema;
    })().optional(), force: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~" }) : schema;
    })(), (() => {
      const schema = z.null().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~" }) : schema;
    })()]).describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~" }) : schema;
    })().optional(), synchronousWatchDirectory: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively." }) : schema;
    })()]).describe("Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively." }) : schema;
    })().optional(), watchDirectory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how directories are watched on systems that lack recursive file-watching functionality.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how directories are watched on systems that lack recursive file-watching functionality." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how directories are watched on systems that lack recursive file-watching functionality.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how directories are watched on systems that lack recursive file-watching functionality." }) : schema;
    })()]).describe("Specify how directories are watched on systems that lack recursive file-watching functionality.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how directories are watched on systems that lack recursive file-watching functionality." }) : schema;
    })().optional(), watchFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how the TypeScript watch mode works.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how the TypeScript watch mode works." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how the TypeScript watch mode works.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how the TypeScript watch mode works." }) : schema;
    })()]).describe("Specify how the TypeScript watch mode works.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how the TypeScript watch mode works." }) : schema;
    })().optional() }).passthrough().describe("Settings for the watch mode in TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Settings for the watch mode in TypeScript." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Settings for the watch mode in TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Settings for the watch mode in TypeScript." }) : schema;
    })()]).describe("Settings for the watch mode in TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Settings for the watch mode in TypeScript." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ buildOptions: z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ assumeChangesOnlyAffectDirectDependencies: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.", default: false }) : schema;
    })()]).describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.", default: false }) : schema;
    })().optional(), dry: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~", default: false }) : schema;
    })()]).describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~", default: false }) : schema;
    })().optional(), force: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Build all projects, including those that appear to be up to date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Build all projects, including those that appear to be up to date", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Build all projects, including those that appear to be up to date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Build all projects, including those that appear to be up to date", default: false }) : schema;
    })()]).describe("Build all projects, including those that appear to be up to date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Build all projects, including those that appear to be up to date", default: false }) : schema;
    })().optional(), incremental: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Save .tsbuildinfo files to allow for incremental compilation of projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Save .tsbuildinfo files to allow for incremental compilation of projects.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Save .tsbuildinfo files to allow for incremental compilation of projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Save .tsbuildinfo files to allow for incremental compilation of projects.", default: false }) : schema;
    })()]).describe("Save .tsbuildinfo files to allow for incremental compilation of projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Save .tsbuildinfo files to allow for incremental compilation of projects.", default: false }) : schema;
    })().optional(), traceResolution: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })()]).describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })().optional(), verbose: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable verbose logging");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable verbose logging", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable verbose logging");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable verbose logging", default: false }) : schema;
    })()]).describe("Enable verbose logging");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable verbose logging", default: false }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }).optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ ["ts-node"]: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ compiler: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify a custom TypeScript compiler.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom TypeScript compiler.", default: "typescript" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a custom TypeScript compiler.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom TypeScript compiler.", default: "typescript" }) : schema;
    })()]).describe("Specify a custom TypeScript compiler.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom TypeScript compiler.", default: "typescript" }) : schema;
    })().optional(), compilerHost: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use TypeScript's compiler host API instead of the language service API.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's compiler host API instead of the language service API.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use TypeScript's compiler host API instead of the language service API.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's compiler host API instead of the language service API.", default: false }) : schema;
    })()]).describe("Use TypeScript's compiler host API instead of the language service API.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's compiler host API instead of the language service API.", default: false }) : schema;
    })().optional(), compilerOptions: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.object({}).passthrough().describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })()]).describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })(), (() => {
      const schema = z.union([(() => {
      const schema = z.object({ allowArbitraryExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })()]).describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })().optional(), allowImportingTsExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })()]).describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })().optional(), allowJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })()]).describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })().optional(), allowSyntheticDefaultImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })()]).describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })().optional(), allowUmdGlobalAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })()]).describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })().optional(), allowUnreachableCode: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })()]).describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })().optional(), allowUnusedLabels: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })()]).describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })().optional(), alwaysStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })()]).describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })().optional(), assumeChangesOnlyAffectDirectDependencies: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })()]).describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })().optional(), baseUrl: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })()]).describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })().optional(), charset: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })()]).describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })().optional(), checkJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })()]).describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })().optional(), composite: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })()]).describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })().optional(), customConditions: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })()]).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })().optional(), declaration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })()]).describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })().optional(), declarationDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })()]).describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })().optional(), declarationMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })()]).describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })().optional(), diagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })()]).describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })().optional(), disableReferencedProjectLoad: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })()]).describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })().optional(), disableSizeLimit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })()]).describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })().optional(), disableSolutionSearching: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })()]).describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })().optional(), disableSourceOfProjectReferenceRedirect: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })()]).describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })().optional(), downlevelIteration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })()]).describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })().optional(), emitBOM: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })()]).describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })().optional(), emitDeclarationOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })()]).describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })().optional(), emitDecoratorMetadata: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })()]).describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })().optional(), erasableSyntaxOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })()]).describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })().optional(), esModuleInterop: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })()]).describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })().optional(), exactOptionalPropertyTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })()]).describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })().optional(), experimentalDecorators: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })()]).describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })().optional(), extendedDiagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })()]).describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })().optional(), fallbackPolling: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "fixedInterval", "priorityInterval", "dynamicPriority", "fixedChunkSize"]).describe("Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later." }) : schema;
    })().optional(), forceConsistentCasingInFileNames: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })()]).describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })().optional(), generateCpuProfile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })()]).describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })().optional(), importHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })()]).describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })().optional(), importsNotUsedAsValues: (() => {
      const schema = z.enum(["remove", "preserve", "error"]).describe("Specify emit/checking behavior for imports that are only used for types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify emit/checking behavior for imports that are only used for types.", default: "remove" }) : schema;
    })().optional(), incremental: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })()]).describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })().optional(), inlineSourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), inlineSources: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), isolatedDeclarations: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })()]).describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })().optional(), isolatedModules: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })()]).describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })().optional(), jsx: (() => {
      const schema = z.enum(["preserve", "react", "react-jsx", "react-jsxdev", "react-native"]).describe("Specify what JSX code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what JSX code is generated." }) : schema;
    })().optional(), jsxFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })()]).describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })().optional(), jsxFragmentFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })()]).describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })().optional(), jsxImportSource: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })()]).describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })().optional(), keyofStringsOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })()]).describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })().optional(), lib: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.intersection(z.string(), z.union([z.enum(["ES5", "ES6", "ES2015", "ES2015.Collection", "ES2015.Core", "ES2015.Generator", "ES2015.Iterable", "ES2015.Promise", "ES2015.Proxy", "ES2015.Reflect", "ES2015.Symbol.WellKnown", "ES2015.Symbol", "ES2016", "ES2016.Array.Include", "ES2017", "ES2017.Intl", "ES2017.Object", "ES2017.SharedMemory", "ES2017.String", "ES2017.TypedArrays", "ES2017.ArrayBuffer", "ES2018", "ES2018.AsyncGenerator", "ES2018.AsyncIterable", "ES2018.Intl", "ES2018.Promise", "ES2018.Regexp", "ES2019", "ES2019.Array", "ES2019.Intl", "ES2019.Object", "ES2019.String", "ES2019.Symbol", "ES2020", "ES2020.BigInt", "ES2020.Promise", "ES2020.String", "ES2020.Symbol.WellKnown", "ESNext", "ESNext.Array", "ESNext.AsyncIterable", "ESNext.BigInt", "ESNext.Collection", "ESNext.Intl", "ESNext.Iterator", "ESNext.Object", "ESNext.Promise", "ESNext.Regexp", "ESNext.String", "ESNext.Symbol", "DOM", "DOM.AsyncIterable", "DOM.Iterable", "ScriptHost", "WebWorker", "WebWorker.AsyncIterable", "WebWorker.ImportScripts", "Webworker.Iterable", "ES7", "ES2021", "ES2020.SharedMemory", "ES2020.Intl", "ES2020.Date", "ES2020.Number", "ES2021.Promise", "ES2021.String", "ES2021.WeakRef", "ESNext.WeakRef", "ES2021.Intl", "ES2022", "ES2022.Array", "ES2022.Error", "ES2022.Intl", "ES2022.Object", "ES2022.String", "ES2022.SharedMemory", "ES2022.RegExp", "ES2023", "ES2023.Array", "ES2024", "ES2024.ArrayBuffer", "ES2024.Collection", "ES2024.Object", "ES2024.Promise", "ES2024.Regexp", "ES2024.SharedMemory", "ES2024.String", "Decorators", "Decorators.Legacy", "ES2017.Date", "ES2023.Collection", "ESNext.Decorators", "ESNext.Disposable", "ESNext.Error", "ESNext.Sharedmemory"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]5|[Ee][Ss]6|[Ee][Ss]7$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2015(\\.([Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Cc][Oo][Rr][Ee]|[Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Pp][Rr][Oo][Xx][Yy]|[Rr][Ee][Ff][Ll][Ee][Cc][Tt]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2016(\\.[Aa][Rr][Rr][Aa][Yy]\\.[Ii][Nn][Cc][Ll][Uu][Dd][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2017(\\.([Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Tt][Yy][Pp][Ee][Dd][Aa][Rr][Rr][Aa][Yy][Ss]|[Dd][Aa][Tt][Ee]|[Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2018(\\.([Aa][Ss][Yy][Nn][Cc][Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2019(\\.([Aa][Rr][Rr][Aa][Yy]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2020(\\.([Bb][Ii][Gg][Ii][Nn][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ii][Nn][Tt][Ll]|[Dd][Aa][Tt][Ee]|[Nn][Uu][Mm][Bb][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2021(\\.([Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2022(\\.([Aa][Rr][Rr][Aa][Yy]|[Ee][Rr][Rr][Oo][Rr]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2023(\\.([Aa][Rr][Rr][Aa][Yy]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2024(\\.([Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss][Nn][Ee][Xx][Tt](\\.([Aa][Rr][Rr][Aa][Yy]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Bb][Ii][Gg][Ii][Nn][Tt]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]|[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss]|[Dd][Ii][Ss][Pp][Oo][Ss][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Oo][Mm](\\.([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ss][Cc][Rr][Ii][Pp][Tt][Hh][Oo][Ss][Tt]$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ww][Ee][Bb][Ww][Oo][Rr][Kk][Ee][Rr](\\.([Ii][Mm][Pp][Oo][Rr][Tt][Ss][Cc][Rr][Ii][Pp][Tt][Ss]|([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss](\\.([Ll][Ee][Gg][Aa][Cc][Yy]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })]))).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })()]).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })().optional(), libReplacement: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })()]).describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })().optional(), listEmittedFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })()]).describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })().optional(), listFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })()]).describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })().optional(), listFilesOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })()]).describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })().optional(), mapRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })()]).describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })().optional(), maxNodeModuleJsDepth: (() => {
      const schema = z.union([(() => {
      const schema = z.number().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })()]).describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })().optional(), module: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })()]).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), z.union([z.enum(["commonjs", "amd", "system", "umd", "es6", "es2015", "es2020", "esnext", "none", "es2022", "node16", "node18", "node20", "nodenext", "preserve"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Cc][Oo][Mm][Mm][Oo][Nn][Jj][Ss]|[AaUu][Mm][Dd]|[Ss][Yy][Ss][Tt][Ee][Mm]|[Ee][Ss]([356]|20(1[567]|2[02])|[Nn][Ee][Xx][Tt])|[Nn][Oo][dD][Ee](1[68]|20)|[Nn][Oo][Dd][Ee][Nn][Ee][Xx][Tt]|[Nn][Oo][Nn][Ee]|[Pp][Rr][Ee][Ss][Ee][Rr][Vv][Ee])$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })().optional(), moduleDetection: (() => {
      const schema = z.enum(["auto", "legacy", "force"]).describe("Control what method is used to detect module-format JS files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Control what method is used to detect module-format JS files." }) : schema;
    })().optional(), moduleResolution: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })()]).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), z.union([z.enum(["classic", "node", "node10", "node16", "nodenext", "bundler"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(([Nn]ode)|([Nn]ode1[06])|([Nn]ode[Nn]ext)|([Cc]lassic)|([Bb]undler))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })().optional(), moduleSuffixes: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })(), (() => {
      const schema = z.null().describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })()]).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })().optional(), newLine: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })()]).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), z.union([z.enum(["crlf", "lf"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(CRLF|LF|crlf|lf)$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })().optional(), noCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })()]).describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })().optional(), noEmit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })()]).describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })().optional(), noEmitHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })()]).describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })().optional(), noEmitOnError: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })()]).describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })().optional(), noErrorTruncation: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })()]).describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })().optional(), noFallthroughCasesInSwitch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })()]).describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })().optional(), noImplicitAny: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })()]).describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })().optional(), noImplicitOverride: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })()]).describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })().optional(), noImplicitReturns: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })()]).describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })().optional(), noImplicitThis: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })()]).describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })().optional(), noImplicitUseStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })().optional(), noLib: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })()]).describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })().optional(), noPropertyAccessFromIndexSignature: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })()]).describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })().optional(), noResolve: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })()]).describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })().optional(), noStrictGenericChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })()]).describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })().optional(), noUncheckedIndexedAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })()]).describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })().optional(), noUncheckedSideEffectImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })()]).describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })().optional(), noUnusedLocals: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })()]).describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })().optional(), noUnusedParameters: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })()]).describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })().optional(), out: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })()]).describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })().optional(), outDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })()]).describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })().optional(), outFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })()]).describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })().optional(), paths: (() => {
      const schema = z.union([(() => {
      const schema = z.record(z.string(), z.union([z.array((() => {
      const schema = z.string().describe("Path mapping to be computed relative to baseUrl option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Path mapping to be computed relative to baseUrl option." }) : schema;
    })()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }), z.null()])).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })()]).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })().optional(), plugins: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.object({ name: (() => {
      const schema = z.string().describe("Plugin name.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Plugin name." }) : schema;
    })().optional() }).passthrough()).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })()]).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })().optional(), preserveConstEnums: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })()]).describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })().optional(), preserveSymlinks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })()]).describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })().optional(), preserveValueImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })()]).describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })().optional(), preserveWatchOutput: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })()]).describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })().optional(), pretty: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })()]).describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })().optional(), reactNamespace: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })()]).describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })().optional(), removeComments: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })()]).describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })().optional(), resolveJsonModule: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })()]).describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })().optional(), resolvePackageJsonExports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })().optional(), resolvePackageJsonImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })().optional(), rewriteRelativeImportExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })()]).describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })().optional(), rootDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })()]).describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })().optional(), rootDirs: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })()]).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })().optional(), skipDefaultLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })()]).describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })().optional(), skipLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })()]).describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })().optional(), sourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })().optional(), sourceRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })()]).describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })().optional(), strict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })()]).describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })().optional(), strictBindCallApply: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })()]).describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })().optional(), strictBuiltinIteratorReturn: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })()]).describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })().optional(), strictFunctionTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })()]).describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })().optional(), strictNullChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })()]).describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })().optional(), strictPropertyInitialization: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })()]).describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })().optional(), stripInternal: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })()]).describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })().optional(), suppressExcessPropertyErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })()]).describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })().optional(), suppressImplicitAnyIndexErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })()]).describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })().optional(), target: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })()]).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), z.union([z.enum(["es3", "es5", "es6", "es2015", "es2016", "es2017", "es2018", "es2019", "es2020", "es2021", "es2022", "es2023", "es2024", "es2025", "esnext"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Ee][Ss]([356]|(20(1[56789]|2[012345]))|[Nn][Ee][Xx][Tt]))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })().optional(), traceResolution: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })()]).describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })().optional(), tsBuildInfoFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })()]).describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })().optional(), typeRoots: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })()]).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })().optional(), types: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })()]).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })().optional(), useDefineForClassFields: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })()]).describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })().optional(), useUnknownInCatchVariables: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })()]).describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })().optional(), verbatimModuleSyntax: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })()]).describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })().optional(), watch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })()]).describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })().optional(), watchDirectory: (() => {
      const schema = z.enum(["useFsEvents", "fixedPollingInterval", "dynamicPriorityPolling", "fixedChunkSizePolling"]).describe("Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional(), watchFile: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "useFsEvents", "useFsEventsOnParentDirectory", "fixedChunkSizePolling"]).describe("Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional() }).passthrough().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })()]).describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })()).describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })().optional(), emit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit output files into `.ts-node` directory.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit output files into `.ts-node` directory.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit output files into `.ts-node` directory.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit output files into `.ts-node` directory.", default: false }) : schema;
    })()]).describe("Emit output files into `.ts-node` directory.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit output files into `.ts-node` directory.", default: false }) : schema;
    })().optional(), esm: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules" }) : schema;
    })()]).describe("Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules" }) : schema;
    })().optional(), experimentalReplAwait: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior." }) : schema;
    })()]).describe("Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior." }) : schema;
    })().optional(), experimentalResolver: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514" }) : schema;
    })()]).describe("Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514" }) : schema;
    })().optional(), experimentalSpecifierResolution: (() => {
      const schema = z.enum(["explicit", "node"]).describe("Like node's `--experimental-specifier-resolution`, , but can also be set in your `tsconfig.json` for convenience.\n\nFor details, see https://nodejs.org/dist/latest-v18.x/docs/api/esm.html#customizing-esm-specifier-resolution-algorithm");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Like node's `--experimental-specifier-resolution`, , but can also be set in your `tsconfig.json` for convenience.\n\nFor details, see https://nodejs.org/dist/latest-v18.x/docs/api/esm.html#customizing-esm-specifier-resolution-algorithm" }) : schema;
    })().optional(), files: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.", default: false }) : schema;
    })()]).describe("Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.", default: false }) : schema;
    })().optional(), ignore: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).describe("Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.", default: ["(?:^|/)node_modules/"] }) : schema;
    })(), (() => {
      const schema = z.null().describe("Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.", default: ["(?:^|/)node_modules/"] }) : schema;
    })()]).describe("Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.", default: ["(?:^|/)node_modules/"] }) : schema;
    })().optional(), ignoreDiagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.union([z.string(), z.number()])).describe("Ignore TypeScript warnings by diagnostic code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ignore TypeScript warnings by diagnostic code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ignore TypeScript warnings by diagnostic code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ignore TypeScript warnings by diagnostic code." }) : schema;
    })()]).describe("Ignore TypeScript warnings by diagnostic code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ignore TypeScript warnings by diagnostic code." }) : schema;
    })().optional(), logError: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Logs TypeScript errors to stderr instead of throwing exceptions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Logs TypeScript errors to stderr instead of throwing exceptions.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Logs TypeScript errors to stderr instead of throwing exceptions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Logs TypeScript errors to stderr instead of throwing exceptions.", default: false }) : schema;
    })()]).describe("Logs TypeScript errors to stderr instead of throwing exceptions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Logs TypeScript errors to stderr instead of throwing exceptions.", default: false }) : schema;
    })().optional(), moduleTypes: z.union([z.object({}).passthrough(), z.null()]).optional(), preferTsExts: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`", default: false }) : schema;
    })()]).describe("Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`", default: false }) : schema;
    })().optional(), pretty: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use pretty diagnostic formatter.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use pretty diagnostic formatter.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use pretty diagnostic formatter.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use pretty diagnostic formatter.", default: false }) : schema;
    })()]).describe("Use pretty diagnostic formatter.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use pretty diagnostic formatter.", default: false }) : schema;
    })().optional(), require: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).describe("Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results." }) : schema;
    })()]).describe("Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results." }) : schema;
    })().optional(), scope: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Scope compiler to files within `scopeDir`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Scope compiler to files within `scopeDir`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Scope compiler to files within `scopeDir`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Scope compiler to files within `scopeDir`.", default: false }) : schema;
    })()]).describe("Scope compiler to files within `scopeDir`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Scope compiler to files within `scopeDir`.", default: false }) : schema;
    })().optional(), scopeDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string();
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { default: "First of: `tsconfig.json` \"rootDir\" if specified, directory containing `tsconfig.json`, or cwd if no `tsconfig.json` is loaded." }) : schema;
    })(), (() => {
      const schema = z.null();
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { default: "First of: `tsconfig.json` \"rootDir\" if specified, directory containing `tsconfig.json`, or cwd if no `tsconfig.json` is loaded." }) : schema;
    })()]);
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { default: "First of: `tsconfig.json` \"rootDir\" if specified, directory containing `tsconfig.json`, or cwd if no `tsconfig.json` is loaded." }) : schema;
    })().optional(), skipIgnore: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip ignore check, so that compilation will be attempted for all files with matching extensions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip ignore check, so that compilation will be attempted for all files with matching extensions.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip ignore check, so that compilation will be attempted for all files with matching extensions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip ignore check, so that compilation will be attempted for all files with matching extensions.", default: false }) : schema;
    })()]).describe("Skip ignore check, so that compilation will be attempted for all files with matching extensions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip ignore check, so that compilation will be attempted for all files with matching extensions.", default: false }) : schema;
    })().optional(), swc: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers" }) : schema;
    })()]).describe("Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers" }) : schema;
    })().optional(), transpileOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use TypeScript's faster `transpileModule`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's faster `transpileModule`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use TypeScript's faster `transpileModule`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's faster `transpileModule`.", default: false }) : schema;
    })()]).describe("Use TypeScript's faster `transpileModule`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's faster `transpileModule`.", default: false }) : schema;
    })().optional(), transpiler: (() => {
      const schema = z.union([z.union([z.array(z.any()).min(2).max(2).superRefine((val, ctx) => {
      const schemas = [z.union([z.string(), z.null()]), z.union([z.object({}).passthrough(), z.null()])];
      for (let i = 0; i < Math.min(val.length, schemas.length); i++) {
        const itemResult = schemas[i].safeParse(val[i]);
        if (!itemResult.success) {
          itemResult.error.issues.forEach(issue => {
            ctx.addIssue({ ...issue, path: [i, ...issue.path] });
          });
        }
      }
    }), z.null()]), z.union([z.string(), z.null()])]).describe("Specify a custom transpiler for use with transpileOnly");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom transpiler for use with transpileOnly" }) : schema;
    })().optional(), typeCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).", default: true }) : schema;
    })()]).describe("**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).", default: true }) : schema;
    })().optional() }).passthrough().describe("ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support." }) : schema;
    })(), (() => {
      const schema = z.null().describe("ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support." }) : schema;
    })()]).describe("ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.union([z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ files: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included." }) : schema;
    })(), (() => {
      const schema = z.null().describe("If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included." }) : schema;
    })()]).describe("If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ exclude: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later." }) : schema;
    })()]).describe("Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ include: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later." }) : schema;
    })()]).describe("Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ references: (() => {
      const schema = z.array((() => {
      const schema = z.object({ path: (() => {
      const schema = z.string().describe("Path to referenced tsconfig or to folder containing tsconfig.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Path to referenced tsconfig or to folder containing tsconfig." }) : schema;
    })().optional() }).passthrough().describe("Project reference.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Project reference." }) : schema;
    })()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Referenced projects. Requires TypeScript version 3.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Referenced projects. Requires TypeScript version 3.0 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])));
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { title: "JSON schema for the TypeScript compiler's configuration file" }) : schema;
    })();
type another_TSConfigType = z.infer<typeof another_TSConfig>;

const another_TSConfigNative = (() => {
      const schema = z.intersection((() => {
      const schema = z.object({}).passthrough();
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { title: "JSON schema for the TypeScript compiler's configuration file" }) : schema;
    })(), z.intersection(z.intersection(z.intersection(z.intersection(z.intersection(z.intersection(z.intersection(z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ compilerOptions: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ allowArbitraryExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })()]).describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })().optional(), allowImportingTsExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })()]).describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })().optional(), allowJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })()]).describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })().optional(), allowSyntheticDefaultImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })()]).describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })().optional(), allowUmdGlobalAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })()]).describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })().optional(), allowUnreachableCode: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })()]).describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })().optional(), allowUnusedLabels: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })()]).describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })().optional(), alwaysStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })()]).describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })().optional(), assumeChangesOnlyAffectDirectDependencies: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })()]).describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })().optional(), baseUrl: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })()]).describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })().optional(), charset: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })()]).describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })().optional(), checkJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })()]).describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })().optional(), composite: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })()]).describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })().optional(), customConditions: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })()]).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })().optional(), declaration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })()]).describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })().optional(), declarationDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })()]).describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })().optional(), declarationMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })()]).describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })().optional(), diagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })()]).describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })().optional(), disableReferencedProjectLoad: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })()]).describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })().optional(), disableSizeLimit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })()]).describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })().optional(), disableSolutionSearching: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })()]).describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })().optional(), disableSourceOfProjectReferenceRedirect: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })()]).describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })().optional(), downlevelIteration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })()]).describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })().optional(), emitBOM: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })()]).describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })().optional(), emitDeclarationOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })()]).describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })().optional(), emitDecoratorMetadata: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })()]).describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })().optional(), erasableSyntaxOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })()]).describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })().optional(), esModuleInterop: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })()]).describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })().optional(), exactOptionalPropertyTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })()]).describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })().optional(), experimentalDecorators: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })()]).describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })().optional(), extendedDiagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })()]).describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })().optional(), fallbackPolling: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "fixedInterval", "priorityInterval", "dynamicPriority", "fixedChunkSize"]).describe("Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later." }) : schema;
    })().optional(), forceConsistentCasingInFileNames: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })()]).describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })().optional(), generateCpuProfile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })()]).describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })().optional(), importHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })()]).describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })().optional(), importsNotUsedAsValues: (() => {
      const schema = z.enum(["remove", "preserve", "error"]).describe("Specify emit/checking behavior for imports that are only used for types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify emit/checking behavior for imports that are only used for types.", default: "remove" }) : schema;
    })().optional(), incremental: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })()]).describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })().optional(), inlineSourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), inlineSources: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), isolatedDeclarations: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })()]).describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })().optional(), isolatedModules: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })()]).describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })().optional(), jsx: (() => {
      const schema = z.enum(["preserve", "react", "react-jsx", "react-jsxdev", "react-native"]).describe("Specify what JSX code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what JSX code is generated." }) : schema;
    })().optional(), jsxFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })()]).describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })().optional(), jsxFragmentFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })()]).describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })().optional(), jsxImportSource: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })()]).describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })().optional(), keyofStringsOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })()]).describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })().optional(), lib: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.intersection(z.string(), z.union([z.enum(["ES5", "ES6", "ES2015", "ES2015.Collection", "ES2015.Core", "ES2015.Generator", "ES2015.Iterable", "ES2015.Promise", "ES2015.Proxy", "ES2015.Reflect", "ES2015.Symbol.WellKnown", "ES2015.Symbol", "ES2016", "ES2016.Array.Include", "ES2017", "ES2017.Intl", "ES2017.Object", "ES2017.SharedMemory", "ES2017.String", "ES2017.TypedArrays", "ES2017.ArrayBuffer", "ES2018", "ES2018.AsyncGenerator", "ES2018.AsyncIterable", "ES2018.Intl", "ES2018.Promise", "ES2018.Regexp", "ES2019", "ES2019.Array", "ES2019.Intl", "ES2019.Object", "ES2019.String", "ES2019.Symbol", "ES2020", "ES2020.BigInt", "ES2020.Promise", "ES2020.String", "ES2020.Symbol.WellKnown", "ESNext", "ESNext.Array", "ESNext.AsyncIterable", "ESNext.BigInt", "ESNext.Collection", "ESNext.Intl", "ESNext.Iterator", "ESNext.Object", "ESNext.Promise", "ESNext.Regexp", "ESNext.String", "ESNext.Symbol", "DOM", "DOM.AsyncIterable", "DOM.Iterable", "ScriptHost", "WebWorker", "WebWorker.AsyncIterable", "WebWorker.ImportScripts", "Webworker.Iterable", "ES7", "ES2021", "ES2020.SharedMemory", "ES2020.Intl", "ES2020.Date", "ES2020.Number", "ES2021.Promise", "ES2021.String", "ES2021.WeakRef", "ESNext.WeakRef", "ES2021.Intl", "ES2022", "ES2022.Array", "ES2022.Error", "ES2022.Intl", "ES2022.Object", "ES2022.String", "ES2022.SharedMemory", "ES2022.RegExp", "ES2023", "ES2023.Array", "ES2024", "ES2024.ArrayBuffer", "ES2024.Collection", "ES2024.Object", "ES2024.Promise", "ES2024.Regexp", "ES2024.SharedMemory", "ES2024.String", "Decorators", "Decorators.Legacy", "ES2017.Date", "ES2023.Collection", "ESNext.Decorators", "ESNext.Disposable", "ESNext.Error", "ESNext.Sharedmemory"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]5|[Ee][Ss]6|[Ee][Ss]7$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2015(\\.([Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Cc][Oo][Rr][Ee]|[Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Pp][Rr][Oo][Xx][Yy]|[Rr][Ee][Ff][Ll][Ee][Cc][Tt]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2016(\\.[Aa][Rr][Rr][Aa][Yy]\\.[Ii][Nn][Cc][Ll][Uu][Dd][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2017(\\.([Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Tt][Yy][Pp][Ee][Dd][Aa][Rr][Rr][Aa][Yy][Ss]|[Dd][Aa][Tt][Ee]|[Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2018(\\.([Aa][Ss][Yy][Nn][Cc][Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2019(\\.([Aa][Rr][Rr][Aa][Yy]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2020(\\.([Bb][Ii][Gg][Ii][Nn][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ii][Nn][Tt][Ll]|[Dd][Aa][Tt][Ee]|[Nn][Uu][Mm][Bb][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2021(\\.([Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2022(\\.([Aa][Rr][Rr][Aa][Yy]|[Ee][Rr][Rr][Oo][Rr]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2023(\\.([Aa][Rr][Rr][Aa][Yy]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2024(\\.([Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss][Nn][Ee][Xx][Tt](\\.([Aa][Rr][Rr][Aa][Yy]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Bb][Ii][Gg][Ii][Nn][Tt]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]|[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss]|[Dd][Ii][Ss][Pp][Oo][Ss][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Oo][Mm](\\.([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ss][Cc][Rr][Ii][Pp][Tt][Hh][Oo][Ss][Tt]$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ww][Ee][Bb][Ww][Oo][Rr][Kk][Ee][Rr](\\.([Ii][Mm][Pp][Oo][Rr][Tt][Ss][Cc][Rr][Ii][Pp][Tt][Ss]|([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss](\\.([Ll][Ee][Gg][Aa][Cc][Yy]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })]))).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })()]).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })().optional(), libReplacement: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })()]).describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })().optional(), listEmittedFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })()]).describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })().optional(), listFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })()]).describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })().optional(), listFilesOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })()]).describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })().optional(), mapRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })()]).describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })().optional(), maxNodeModuleJsDepth: (() => {
      const schema = z.union([(() => {
      const schema = z.number().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })()]).describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })().optional(), module: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })()]).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), z.union([z.enum(["commonjs", "amd", "system", "umd", "es6", "es2015", "es2020", "esnext", "none", "es2022", "node16", "node18", "node20", "nodenext", "preserve"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Cc][Oo][Mm][Mm][Oo][Nn][Jj][Ss]|[AaUu][Mm][Dd]|[Ss][Yy][Ss][Tt][Ee][Mm]|[Ee][Ss]([356]|20(1[567]|2[02])|[Nn][Ee][Xx][Tt])|[Nn][Oo][dD][Ee](1[68]|20)|[Nn][Oo][Dd][Ee][Nn][Ee][Xx][Tt]|[Nn][Oo][Nn][Ee]|[Pp][Rr][Ee][Ss][Ee][Rr][Vv][Ee])$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })().optional(), moduleDetection: (() => {
      const schema = z.enum(["auto", "legacy", "force"]).describe("Control what method is used to detect module-format JS files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Control what method is used to detect module-format JS files." }) : schema;
    })().optional(), moduleResolution: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })()]).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), z.union([z.enum(["classic", "node", "node10", "node16", "nodenext", "bundler"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(([Nn]ode)|([Nn]ode1[06])|([Nn]ode[Nn]ext)|([Cc]lassic)|([Bb]undler))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })().optional(), moduleSuffixes: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })(), (() => {
      const schema = z.null().describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })()]).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })().optional(), newLine: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })()]).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), z.union([z.enum(["crlf", "lf"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(CRLF|LF|crlf|lf)$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })().optional(), noCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })()]).describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })().optional(), noEmit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })()]).describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })().optional(), noEmitHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })()]).describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })().optional(), noEmitOnError: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })()]).describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })().optional(), noErrorTruncation: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })()]).describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })().optional(), noFallthroughCasesInSwitch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })()]).describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })().optional(), noImplicitAny: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })()]).describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })().optional(), noImplicitOverride: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })()]).describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })().optional(), noImplicitReturns: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })()]).describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })().optional(), noImplicitThis: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })()]).describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })().optional(), noImplicitUseStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })().optional(), noLib: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })()]).describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })().optional(), noPropertyAccessFromIndexSignature: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })()]).describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })().optional(), noResolve: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })()]).describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })().optional(), noStrictGenericChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })()]).describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })().optional(), noUncheckedIndexedAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })()]).describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })().optional(), noUncheckedSideEffectImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })()]).describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })().optional(), noUnusedLocals: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })()]).describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })().optional(), noUnusedParameters: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })()]).describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })().optional(), out: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })()]).describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })().optional(), outDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })()]).describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })().optional(), outFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })()]).describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })().optional(), paths: (() => {
      const schema = z.union([(() => {
      const schema = z.record(z.string(), z.union([z.array((() => {
      const schema = z.string().describe("Path mapping to be computed relative to baseUrl option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Path mapping to be computed relative to baseUrl option." }) : schema;
    })()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }), z.null()])).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })()]).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })().optional(), plugins: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.object({ name: (() => {
      const schema = z.string().describe("Plugin name.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Plugin name." }) : schema;
    })().optional() }).passthrough()).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })()]).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })().optional(), preserveConstEnums: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })()]).describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })().optional(), preserveSymlinks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })()]).describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })().optional(), preserveValueImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })()]).describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })().optional(), preserveWatchOutput: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })()]).describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })().optional(), pretty: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })()]).describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })().optional(), reactNamespace: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })()]).describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })().optional(), removeComments: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })()]).describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })().optional(), resolveJsonModule: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })()]).describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })().optional(), resolvePackageJsonExports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })().optional(), resolvePackageJsonImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })().optional(), rewriteRelativeImportExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })()]).describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })().optional(), rootDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })()]).describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })().optional(), rootDirs: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })()]).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })().optional(), skipDefaultLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })()]).describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })().optional(), skipLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })()]).describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })().optional(), sourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })().optional(), sourceRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })()]).describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })().optional(), strict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })()]).describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })().optional(), strictBindCallApply: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })()]).describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })().optional(), strictBuiltinIteratorReturn: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })()]).describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })().optional(), strictFunctionTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })()]).describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })().optional(), strictNullChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })()]).describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })().optional(), strictPropertyInitialization: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })()]).describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })().optional(), stripInternal: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })()]).describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })().optional(), suppressExcessPropertyErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })()]).describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })().optional(), suppressImplicitAnyIndexErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })()]).describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })().optional(), target: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })()]).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), z.union([z.enum(["es3", "es5", "es6", "es2015", "es2016", "es2017", "es2018", "es2019", "es2020", "es2021", "es2022", "es2023", "es2024", "es2025", "esnext"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Ee][Ss]([356]|(20(1[56789]|2[012345]))|[Nn][Ee][Xx][Tt]))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })().optional(), traceResolution: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })()]).describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })().optional(), tsBuildInfoFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })()]).describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })().optional(), typeRoots: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })()]).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })().optional(), types: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })()]).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })().optional(), useDefineForClassFields: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })()]).describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })().optional(), useUnknownInCatchVariables: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })()]).describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })().optional(), verbatimModuleSyntax: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })()]).describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })().optional(), watch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })()]).describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })().optional(), watchDirectory: (() => {
      const schema = z.enum(["useFsEvents", "fixedPollingInterval", "dynamicPriorityPolling", "fixedChunkSizePolling"]).describe("Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional(), watchFile: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "useFsEvents", "useFsEventsOnParentDirectory", "fixedChunkSizePolling"]).describe("Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional() }).passthrough().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })()]).describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ compileOnSave: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable Compile-on-Save for this project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable Compile-on-Save for this project." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable Compile-on-Save for this project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable Compile-on-Save for this project." }) : schema;
    })()]).describe("Enable Compile-on-Save for this project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable Compile-on-Save for this project." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ typeAcquisition: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ enable: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable auto type acquisition");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable auto type acquisition", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable auto type acquisition");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable auto type acquisition", default: false }) : schema;
    })()]).describe("Enable auto type acquisition");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable auto type acquisition", default: false }) : schema;
    })().optional(), exclude: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })()]).describe("Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be excluded from auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })().optional(), include: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })()]).describe("Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of type declarations to be included in auto type acquisition. Ex. [\"jquery\", \"lodash\"]" }) : schema;
    })().optional() }).passthrough().describe("Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later." }) : schema;
    })()]).describe("Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Auto type (.d.ts) acquisition options for this project. Requires TypeScript version 2.1 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ allOf: z.any().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ watchOptions: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ excludeDirectories: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Remove a list of directories from the watch process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of directories from the watch process." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove a list of directories from the watch process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of directories from the watch process." }) : schema;
    })()]).describe("Remove a list of directories from the watch process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of directories from the watch process." }) : schema;
    })().optional(), excludeFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Remove a list of files from the watch mode's processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of files from the watch mode's processing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove a list of files from the watch mode's processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of files from the watch mode's processing." }) : schema;
    })()]).describe("Remove a list of files from the watch mode's processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove a list of files from the watch mode's processing." }) : schema;
    })().optional(), fallbackPolling: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify what approach the watcher should use if the system runs out of native file watchers.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what approach the watcher should use if the system runs out of native file watchers." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify what approach the watcher should use if the system runs out of native file watchers.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what approach the watcher should use if the system runs out of native file watchers." }) : schema;
    })()]).describe("Specify what approach the watcher should use if the system runs out of native file watchers.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what approach the watcher should use if the system runs out of native file watchers." }) : schema;
    })().optional(), force: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~" }) : schema;
    })(), (() => {
      const schema = z.null().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~" }) : schema;
    })()]).describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~" }) : schema;
    })().optional(), synchronousWatchDirectory: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively." }) : schema;
    })()]).describe("Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively." }) : schema;
    })().optional(), watchDirectory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how directories are watched on systems that lack recursive file-watching functionality.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how directories are watched on systems that lack recursive file-watching functionality." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how directories are watched on systems that lack recursive file-watching functionality.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how directories are watched on systems that lack recursive file-watching functionality." }) : schema;
    })()]).describe("Specify how directories are watched on systems that lack recursive file-watching functionality.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how directories are watched on systems that lack recursive file-watching functionality." }) : schema;
    })().optional(), watchFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how the TypeScript watch mode works.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how the TypeScript watch mode works." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how the TypeScript watch mode works.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how the TypeScript watch mode works." }) : schema;
    })()]).describe("Specify how the TypeScript watch mode works.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how the TypeScript watch mode works." }) : schema;
    })().optional() }).passthrough().describe("Settings for the watch mode in TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Settings for the watch mode in TypeScript." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Settings for the watch mode in TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Settings for the watch mode in TypeScript." }) : schema;
    })()]).describe("Settings for the watch mode in TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Settings for the watch mode in TypeScript." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ buildOptions: z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ assumeChangesOnlyAffectDirectDependencies: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.", default: false }) : schema;
    })()]).describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.", default: false }) : schema;
    })().optional(), dry: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~", default: false }) : schema;
    })()]).describe("~");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "~", default: false }) : schema;
    })().optional(), force: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Build all projects, including those that appear to be up to date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Build all projects, including those that appear to be up to date", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Build all projects, including those that appear to be up to date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Build all projects, including those that appear to be up to date", default: false }) : schema;
    })()]).describe("Build all projects, including those that appear to be up to date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Build all projects, including those that appear to be up to date", default: false }) : schema;
    })().optional(), incremental: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Save .tsbuildinfo files to allow for incremental compilation of projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Save .tsbuildinfo files to allow for incremental compilation of projects.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Save .tsbuildinfo files to allow for incremental compilation of projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Save .tsbuildinfo files to allow for incremental compilation of projects.", default: false }) : schema;
    })()]).describe("Save .tsbuildinfo files to allow for incremental compilation of projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Save .tsbuildinfo files to allow for incremental compilation of projects.", default: false }) : schema;
    })().optional(), traceResolution: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })()]).describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })().optional(), verbose: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable verbose logging");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable verbose logging", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable verbose logging");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable verbose logging", default: false }) : schema;
    })()]).describe("Enable verbose logging");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable verbose logging", default: false }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }).optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ ["ts-node"]: (() => {
      const schema = z.union([(() => {
      const schema = z.object({ compiler: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify a custom TypeScript compiler.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom TypeScript compiler.", default: "typescript" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a custom TypeScript compiler.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom TypeScript compiler.", default: "typescript" }) : schema;
    })()]).describe("Specify a custom TypeScript compiler.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom TypeScript compiler.", default: "typescript" }) : schema;
    })().optional(), compilerHost: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use TypeScript's compiler host API instead of the language service API.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's compiler host API instead of the language service API.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use TypeScript's compiler host API instead of the language service API.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's compiler host API instead of the language service API.", default: false }) : schema;
    })()]).describe("Use TypeScript's compiler host API instead of the language service API.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's compiler host API instead of the language service API.", default: false }) : schema;
    })().optional(), compilerOptions: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.object({}).passthrough().describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })()]).describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })(), (() => {
      const schema = z.union([(() => {
      const schema = z.object({ allowArbitraryExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })()]).describe("Enable importing files with any extension, provided a declaration file is present.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing files with any extension, provided a declaration file is present." }) : schema;
    })().optional(), allowImportingTsExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })()]).describe("Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow imports to include TypeScript file extensions. Requires `--moduleResolution bundler` and either `--noEmit` or `--emitDeclarationOnly` to be set." }) : schema;
    })().optional(), allowJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })()]).describe("Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow JavaScript files to be a part of your program. Use the `checkJs` option to get errors from these files.", default: false }) : schema;
    })().optional(), allowSyntheticDefaultImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })()]).describe("Allow `import x from y` when a module doesn't have a default export.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow `import x from y` when a module doesn't have a default export." }) : schema;
    })().optional(), allowUmdGlobalAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })()]).describe("Allow accessing UMD globals from modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow accessing UMD globals from modules.", default: false }) : schema;
    })().optional(), allowUnreachableCode: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })()]).describe("Disable error reporting for unreachable code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unreachable code." }) : schema;
    })().optional(), allowUnusedLabels: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })()]).describe("Disable error reporting for unused labels.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable error reporting for unused labels." }) : schema;
    })().optional(), alwaysStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })()]).describe("Ensure `use strict` is always emitted.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure `use strict` is always emitted." }) : schema;
    })().optional(), assumeChangesOnlyAffectDirectDependencies: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })()]).describe("Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Have recompiles in projects that use `incremental` and `watch` mode assume that changes within a file will only affect files directly depending on it." }) : schema;
    })().optional(), baseUrl: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })()]).describe("Specify the base directory to resolve non-relative module names.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the base directory to resolve non-relative module names." }) : schema;
    })().optional(), charset: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })()]).describe("No longer supported. In early versions, manually set the text encoding for reading files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "No longer supported. In early versions, manually set the text encoding for reading files." }) : schema;
    })().optional(), checkJs: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })()]).describe("Enable error reporting in type-checked JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting in type-checked JavaScript files.", default: false }) : schema;
    })().optional(), composite: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })()]).describe("Enable constraints that allow a TypeScript project to be used with project references.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable constraints that allow a TypeScript project to be used with project references.", default: true }) : schema;
    })().optional(), customConditions: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })()]).describe("Conditions to set in addition to the resolver-specific defaults when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Conditions to set in addition to the resolver-specific defaults when resolving imports." }) : schema;
    })().optional(), declaration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })()]).describe("Generate .d.ts files from TypeScript and JavaScript files in your project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Generate .d.ts files from TypeScript and JavaScript files in your project.", default: false }) : schema;
    })().optional(), declarationDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })()]).describe("Specify the output directory for generated declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the output directory for generated declaration files." }) : schema;
    })().optional(), declarationMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })()]).describe("Create sourcemaps for d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create sourcemaps for d.ts files.", default: false }) : schema;
    })().optional(), diagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })()]).describe("Output compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output compiler performance information after building." }) : schema;
    })().optional(), disableReferencedProjectLoad: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })()]).describe("Reduce the number of projects loaded automatically by TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Reduce the number of projects loaded automatically by TypeScript." }) : schema;
    })().optional(), disableSizeLimit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })()]).describe("Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server.", default: false }) : schema;
    })().optional(), disableSolutionSearching: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })()]).describe("Opt a project out of multi-project reference checking when editing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Opt a project out of multi-project reference checking when editing." }) : schema;
    })().optional(), disableSourceOfProjectReferenceRedirect: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })()]).describe("Disable preferring source files instead of declaration files when referencing composite projects.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable preferring source files instead of declaration files when referencing composite projects." }) : schema;
    })().optional(), downlevelIteration: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })()]).describe("Emit more compliant, but verbose and less performant JavaScript for iteration.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit more compliant, but verbose and less performant JavaScript for iteration.", default: false }) : schema;
    })().optional(), emitBOM: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })()]).describe("Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files.", default: false }) : schema;
    })().optional(), emitDeclarationOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })()]).describe("Only output d.ts files and not JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Only output d.ts files and not JavaScript files.", default: false }) : schema;
    })().optional(), emitDecoratorMetadata: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })()]).describe("Emit design-type metadata for decorated declarations in source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit design-type metadata for decorated declarations in source files." }) : schema;
    })().optional(), erasableSyntaxOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })()]).describe("Do not allow runtime constructs that are not part of ECMAScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not allow runtime constructs that are not part of ECMAScript.", default: false }) : schema;
    })().optional(), esModuleInterop: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })()]).describe("Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit additional JavaScript to ease support for importing CommonJS modules. This enables `allowSyntheticDefaultImports` for type compatibility.", default: false }) : schema;
    })().optional(), exactOptionalPropertyTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })()]).describe("Interpret optional property types as written, rather than adding `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Interpret optional property types as written, rather than adding `undefined`.", default: false }) : schema;
    })().optional(), experimentalDecorators: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })()]).describe("Enable experimental support for legacy experimental decorators.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental support for legacy experimental decorators." }) : schema;
    })().optional(), extendedDiagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })()]).describe("Output more detailed compiler performance information after building.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Output more detailed compiler performance information after building.", default: false }) : schema;
    })().optional(), fallbackPolling: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "fixedInterval", "priorityInterval", "dynamicPriority", "fixedChunkSize"]).describe("Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the polling strategy to use when the system runs out of or doesn't support native file watchers. Requires TypeScript version 3.8 or later." }) : schema;
    })().optional(), forceConsistentCasingInFileNames: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })()]).describe("Ensure that casing is correct in imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that casing is correct in imports.", default: true }) : schema;
    })().optional(), generateCpuProfile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })()]).describe("Emit a v8 CPU profile of the compiler run for debugging.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit a v8 CPU profile of the compiler run for debugging.", default: "profile.cpuprofile" }) : schema;
    })().optional(), importHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })()]).describe("Allow importing helper functions from tslib once per project, instead of including them per-file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow importing helper functions from tslib once per project, instead of including them per-file.", default: false }) : schema;
    })().optional(), importsNotUsedAsValues: (() => {
      const schema = z.enum(["remove", "preserve", "error"]).describe("Specify emit/checking behavior for imports that are only used for types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify emit/checking behavior for imports that are only used for types.", default: "remove" }) : schema;
    })().optional(), incremental: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })()]).describe("Enable incremental compilation. Requires TypeScript version 3.4 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable incremental compilation. Requires TypeScript version 3.4 or later." }) : schema;
    })().optional(), inlineSourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include sourcemap files inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include sourcemap files inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), inlineSources: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })()]).describe("Include source code in the sourcemaps inside the emitted JavaScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Include source code in the sourcemaps inside the emitted JavaScript.", default: false }) : schema;
    })().optional(), isolatedDeclarations: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })()]).describe("Require sufficient annotation on exports so other tools can trivially generate declaration files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Require sufficient annotation on exports so other tools can trivially generate declaration files.", default: false }) : schema;
    })().optional(), isolatedModules: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })()]).describe("Ensure that each file can be safely transpiled without relying on other imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure that each file can be safely transpiled without relying on other imports.", default: false }) : schema;
    })().optional(), jsx: (() => {
      const schema = z.enum(["preserve", "react", "react-jsx", "react-jsxdev", "react-native"]).describe("Specify what JSX code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what JSX code is generated." }) : schema;
    })().optional(), jsxFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })()]).describe("Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX factory function used when targeting React JSX emit, e.g. `React.createElement` or `h`.", default: "React.createElement" }) : schema;
    })().optional(), jsxFragmentFactory: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })()]).describe("Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. `React.Fragment` or `Fragment`.", default: "React.Fragment" }) : schema;
    })().optional(), jsxImportSource: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })()]).describe("Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify module specifier used to import the JSX factory functions when using `jsx: react-jsx*`.", default: "react" }) : schema;
    })().optional(), keyofStringsOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })()]).describe("Make keyof only return strings instead of string, numbers or symbols. Legacy option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Make keyof only return strings instead of string, numbers or symbols. Legacy option.", default: false }) : schema;
    })().optional(), lib: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.intersection(z.string(), z.union([z.enum(["ES5", "ES6", "ES2015", "ES2015.Collection", "ES2015.Core", "ES2015.Generator", "ES2015.Iterable", "ES2015.Promise", "ES2015.Proxy", "ES2015.Reflect", "ES2015.Symbol.WellKnown", "ES2015.Symbol", "ES2016", "ES2016.Array.Include", "ES2017", "ES2017.Intl", "ES2017.Object", "ES2017.SharedMemory", "ES2017.String", "ES2017.TypedArrays", "ES2017.ArrayBuffer", "ES2018", "ES2018.AsyncGenerator", "ES2018.AsyncIterable", "ES2018.Intl", "ES2018.Promise", "ES2018.Regexp", "ES2019", "ES2019.Array", "ES2019.Intl", "ES2019.Object", "ES2019.String", "ES2019.Symbol", "ES2020", "ES2020.BigInt", "ES2020.Promise", "ES2020.String", "ES2020.Symbol.WellKnown", "ESNext", "ESNext.Array", "ESNext.AsyncIterable", "ESNext.BigInt", "ESNext.Collection", "ESNext.Intl", "ESNext.Iterator", "ESNext.Object", "ESNext.Promise", "ESNext.Regexp", "ESNext.String", "ESNext.Symbol", "DOM", "DOM.AsyncIterable", "DOM.Iterable", "ScriptHost", "WebWorker", "WebWorker.AsyncIterable", "WebWorker.ImportScripts", "Webworker.Iterable", "ES7", "ES2021", "ES2020.SharedMemory", "ES2020.Intl", "ES2020.Date", "ES2020.Number", "ES2021.Promise", "ES2021.String", "ES2021.WeakRef", "ESNext.WeakRef", "ES2021.Intl", "ES2022", "ES2022.Array", "ES2022.Error", "ES2022.Intl", "ES2022.Object", "ES2022.String", "ES2022.SharedMemory", "ES2022.RegExp", "ES2023", "ES2023.Array", "ES2024", "ES2024.ArrayBuffer", "ES2024.Collection", "ES2024.Object", "ES2024.Promise", "ES2024.Regexp", "ES2024.SharedMemory", "ES2024.String", "Decorators", "Decorators.Legacy", "ES2017.Date", "ES2023.Collection", "ESNext.Decorators", "ESNext.Disposable", "ESNext.Error", "ESNext.Sharedmemory"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]5|[Ee][Ss]6|[Ee][Ss]7$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2015(\\.([Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Cc][Oo][Rr][Ee]|[Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Pp][Rr][Oo][Xx][Yy]|[Rr][Ee][Ff][Ll][Ee][Cc][Tt]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2016(\\.[Aa][Rr][Rr][Aa][Yy]\\.[Ii][Nn][Cc][Ll][Uu][Dd][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2017(\\.([Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Tt][Yy][Pp][Ee][Dd][Aa][Rr][Rr][Aa][Yy][Ss]|[Dd][Aa][Tt][Ee]|[Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2018(\\.([Aa][Ss][Yy][Nn][Cc][Gg][Ee][Nn][Ee][Rr][Aa][Tt][Oo][Rr]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2019(\\.([Aa][Rr][Rr][Aa][Yy]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2020(\\.([Bb][Ii][Gg][Ii][Nn][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]\\.[Ww][Ee][Ll][Ll][Kk][Nn][Oo][Ww][Nn]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ii][Nn][Tt][Ll]|[Dd][Aa][Tt][Ee]|[Nn][Uu][Mm][Bb][Ee][Rr]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2021(\\.([Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2022(\\.([Aa][Rr][Rr][Aa][Yy]|[Ee][Rr][Rr][Oo][Rr]|[Ii][Nn][Tt][Ll]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Rr][Ee][Gg][Ee][Xx][Pp]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2023(\\.([Aa][Rr][Rr][Aa][Yy]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss]2024(\\.([Aa][Rr][Rr][Aa][Yy][Bb][Uu][Ff][Ff][Ee][Rr]|[Cc][Oo][Ll][Ll][Ee][Cc][Tt][Ii][Oo][Nn]|[Oo][Bb][Jj][Ee][Cc][Tt]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Rr][Ee][Gg][Ee][Xx][Pp]|[Ss][Hh][Aa][Rr][Ee][Dd][Mm][Ee][Mm][Oo][Rr][Yy]|[Ss][Tt][Rr][Ii][Nn][Gg]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ee][Ss][Nn][Ee][Xx][Tt](\\.([Aa][Rr][Rr][Aa][Yy]|[Aa][Ss][Yy][Nn][Cc][Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]|[Bb][Ii][Gg][Ii][Nn][Tt]|[Ii][Nn][Tt][Ll]|[Pp][Rr][Oo][Mm][Ii][Ss][Ee]|[Ss][Tt][Rr][Ii][Nn][Gg]|[Ss][Yy][Mm][Bb][Oo][Ll]|[Ww][Ee][Aa][Kk][Rr][Ee][Ff]|[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss]|[Dd][Ii][Ss][Pp][Oo][Ss][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Oo][Mm](\\.([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee])?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ss][Cc][Rr][Ii][Pp][Tt][Hh][Oo][Ss][Tt]$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Ww][Ee][Bb][Ww][Oo][Rr][Kk][Ee][Rr](\\.([Ii][Mm][Pp][Oo][Rr][Tt][Ss][Cc][Rr][Ii][Pp][Tt][Ss]|([Aa][Ss][Yy][Nn][Cc])?[Ii][Tt][Ee][Rr][Aa][Bb][Ll][Ee]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^[Dd][Ee][Cc][Oo][Rr][Aa][Tt][Oo][Rr][Ss](\\.([Ll][Ee][Gg][Aa][Cc][Yy]))?$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })]))).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })()]).describe("Specify a set of bundled library declaration files that describe the target runtime environment.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of bundled library declaration files that describe the target runtime environment." }) : schema;
    })().optional(), libReplacement: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })()]).describe("Enable lib replacement.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable lib replacement.", default: true }) : schema;
    })().optional(), listEmittedFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })()]).describe("Print the names of emitted files after a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print the names of emitted files after a compilation.", default: false }) : schema;
    })().optional(), listFiles: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })()]).describe("Print all of the files read during the compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print all of the files read during the compilation.", default: false }) : schema;
    })().optional(), listFilesOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })()]).describe("Print names of files that are part of the compilation and then stop processing.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Print names of files that are part of the compilation and then stop processing." }) : schema;
    })().optional(), mapRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })()]).describe("Specify the location where debugger should locate map files instead of generated locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the location where debugger should locate map files instead of generated locations." }) : schema;
    })().optional(), maxNodeModuleJsDepth: (() => {
      const schema = z.union([(() => {
      const schema = z.number().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })()]).describe("Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the maximum folder depth used for checking JavaScript files from `node_modules`. Only applicable with `allowJs`.", default: 0 }) : schema;
    })().optional(), module: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })()]).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })(), z.union([z.enum(["commonjs", "amd", "system", "umd", "es6", "es2015", "es2020", "esnext", "none", "es2022", "node16", "node18", "node20", "nodenext", "preserve"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Cc][Oo][Mm][Mm][Oo][Nn][Jj][Ss]|[AaUu][Mm][Dd]|[Ss][Yy][Ss][Tt][Ee][Mm]|[Ee][Ss]([356]|20(1[567]|2[02])|[Nn][Ee][Xx][Tt])|[Nn][Oo][dD][Ee](1[68]|20)|[Nn][Oo][Dd][Ee][Nn][Ee][Xx][Tt]|[Nn][Oo][Nn][Ee]|[Pp][Rr][Ee][Ss][Ee][Rr][Vv][Ee])$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify what module code is generated.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify what module code is generated." }) : schema;
    })().optional(), moduleDetection: (() => {
      const schema = z.enum(["auto", "legacy", "force"]).describe("Control what method is used to detect module-format JS files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Control what method is used to detect module-format JS files." }) : schema;
    })().optional(), moduleResolution: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })()]).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })(), z.union([z.enum(["classic", "node", "node10", "node16", "nodenext", "bundler"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(([Nn]ode)|([Nn]ode1[06])|([Nn]ode[Nn]ext)|([Cc]lassic)|([Bb]undler))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Specify how TypeScript looks up a file from a given module specifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify how TypeScript looks up a file from a given module specifier." }) : schema;
    })().optional(), moduleSuffixes: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })(), (() => {
      const schema = z.null().describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })()]).describe("List of file name suffixes to search when resolving a module.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "List of file name suffixes to search when resolving a module." }) : schema;
    })().optional(), newLine: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })()]).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })(), z.union([z.enum(["crlf", "lf"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^(CRLF|LF|crlf|lf)$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the newline character for emitting files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the newline character for emitting files.", default: "lf" }) : schema;
    })().optional(), noCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })()]).describe("Disable full type checking (only critical parse and emit errors will be reported).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable full type checking (only critical parse and emit errors will be reported).", default: false }) : schema;
    })().optional(), noEmit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })()]).describe("Disable emitting files from a compilation.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files from a compilation.", default: false }) : schema;
    })().optional(), noEmitHelpers: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })()]).describe("Disable generating custom helper functions like `__extends` in compiled output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable generating custom helper functions like `__extends` in compiled output.", default: false }) : schema;
    })().optional(), noEmitOnError: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })()]).describe("Disable emitting files if any type checking errors are reported.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting files if any type checking errors are reported.", default: false }) : schema;
    })().optional(), noErrorTruncation: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })()]).describe("Disable truncating types in error messages.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable truncating types in error messages.", default: false }) : schema;
    })().optional(), noFallthroughCasesInSwitch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })()]).describe("Enable error reporting for fallthrough cases in switch statements.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for fallthrough cases in switch statements.", default: false }) : schema;
    })().optional(), noImplicitAny: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })()]).describe("Enable error reporting for expressions and declarations with an implied `any` type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for expressions and declarations with an implied `any` type." }) : schema;
    })().optional(), noImplicitOverride: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })()]).describe("Ensure overriding members in derived classes are marked with an override modifier.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ensure overriding members in derived classes are marked with an override modifier.", default: false }) : schema;
    })().optional(), noImplicitReturns: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })()]).describe("Enable error reporting for codepaths that do not explicitly return in a function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting for codepaths that do not explicitly return in a function.", default: false }) : schema;
    })().optional(), noImplicitThis: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })()]).describe("Enable error reporting when `this` is given the type `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when `this` is given the type `any`." }) : schema;
    })().optional(), noImplicitUseStrict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Disable adding `use strict` directives in emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable adding `use strict` directives in emitted JavaScript files.", default: false }) : schema;
    })().optional(), noLib: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })()]).describe("Disable including any library files, including the default lib.d.ts.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable including any library files, including the default lib.d.ts.", default: false }) : schema;
    })().optional(), noPropertyAccessFromIndexSignature: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })()]).describe("Enforces using indexed accessors for keys declared using an indexed type.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enforces using indexed accessors for keys declared using an indexed type." }) : schema;
    })().optional(), noResolve: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })()]).describe("Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disallow `import`s, `require`s or `<reference>`s from expanding the number of files TypeScript should add to a project.", default: false }) : schema;
    })().optional(), noStrictGenericChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })()]).describe("Disable strict checking of generic signatures in function types.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable strict checking of generic signatures in function types.", default: false }) : schema;
    })().optional(), noUncheckedIndexedAccess: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })()]).describe("Add `undefined` to a type when accessed using an index.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Add `undefined` to a type when accessed using an index." }) : schema;
    })().optional(), noUncheckedSideEffectImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })()]).describe("Check side effect imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check side effect imports.", default: false }) : schema;
    })().optional(), noUnusedLocals: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })()]).describe("Enable error reporting when local variables aren't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable error reporting when local variables aren't read.", default: false }) : schema;
    })().optional(), noUnusedParameters: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })()]).describe("Raise an error when a function parameter isn't read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Raise an error when a function parameter isn't read.", default: false }) : schema;
    })().optional(), out: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })()]).describe("Deprecated setting. Use `outFile` instead.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Deprecated setting. Use `outFile` instead." }) : schema;
    })().optional(), outDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })()]).describe("Specify an output folder for all emitted files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify an output folder for all emitted files." }) : schema;
    })().optional(), outFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })()]).describe("Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a file that bundles all outputs into one JavaScript file. If `declaration` is true, also designates a file that bundles all .d.ts output." }) : schema;
    })().optional(), paths: (() => {
      const schema = z.union([(() => {
      const schema = z.record(z.string(), z.union([z.array((() => {
      const schema = z.string().describe("Path mapping to be computed relative to baseUrl option.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Path mapping to be computed relative to baseUrl option." }) : schema;
    })()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }), z.null()])).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })()]).describe("Specify a set of entries that re-map imports to additional lookup locations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a set of entries that re-map imports to additional lookup locations." }) : schema;
    })().optional(), plugins: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.object({ name: (() => {
      const schema = z.string().describe("Plugin name.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Plugin name." }) : schema;
    })().optional() }).passthrough()).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })()]).describe("Specify a list of language service plugins to include.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a list of language service plugins to include." }) : schema;
    })().optional(), preserveConstEnums: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })()]).describe("Disable erasing `const enum` declarations in generated code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable erasing `const enum` declarations in generated code.", default: false }) : schema;
    })().optional(), preserveSymlinks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })()]).describe("Disable resolving symlinks to their realpath. This correlates to the same flag in node.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable resolving symlinks to their realpath. This correlates to the same flag in node.", default: false }) : schema;
    })().optional(), preserveValueImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })()]).describe("Preserve unused imported values in the JavaScript output that would otherwise be removed.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Preserve unused imported values in the JavaScript output that would otherwise be removed.", default: false }) : schema;
    })().optional(), preserveWatchOutput: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })()]).describe("Disable wiping the console in watch mode.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable wiping the console in watch mode." }) : schema;
    })().optional(), pretty: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })()]).describe("Enable color and formatting in TypeScript's output to make compiler errors easier to read.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable color and formatting in TypeScript's output to make compiler errors easier to read.", default: true }) : schema;
    })().optional(), reactNamespace: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })()]).describe("Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the object invoked for `createElement`. This only applies when targeting `react` JSX emit.", default: "React" }) : schema;
    })().optional(), removeComments: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })()]).describe("Disable emitting comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting comments.", default: false }) : schema;
    })().optional(), resolveJsonModule: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })()]).describe("Enable importing .json files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable importing .json files.", default: false }) : schema;
    })().optional(), resolvePackageJsonExports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'exports' field when resolving package imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'exports' field when resolving package imports.", default: false }) : schema;
    })().optional(), resolvePackageJsonImports: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })()]).describe("Use the package.json 'imports' field when resolving imports.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use the package.json 'imports' field when resolving imports.", default: false }) : schema;
    })().optional(), rewriteRelativeImportExtensions: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })()]).describe("Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Rewrite `.ts`, `.tsx`, `.mts`, and `.cts` file extensions in relative import paths to their JavaScript equivalent in output files.", default: false }) : schema;
    })().optional(), rootDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })()]).describe("Specify the root folder within your source files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root folder within your source files." }) : schema;
    })().optional(), rootDirs: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })()]).describe("Allow multiple folders to be treated as one when resolving modules.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allow multiple folders to be treated as one when resolving modules." }) : schema;
    })().optional(), skipDefaultLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })()]).describe("Skip type checking .d.ts files that are included with TypeScript.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking .d.ts files that are included with TypeScript.", default: false }) : schema;
    })().optional(), skipLibCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })()]).describe("Skip type checking all .d.ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip type checking all .d.ts files.", default: false }) : schema;
    })().optional(), sourceMap: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })()]).describe("Create source map files for emitted JavaScript files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Create source map files for emitted JavaScript files.", default: false }) : schema;
    })().optional(), sourceRoot: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })()]).describe("Specify the root path for debuggers to find the reference source code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the root path for debuggers to find the reference source code." }) : schema;
    })().optional(), strict: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })()]).describe("Enable all strict type-checking options.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable all strict type-checking options.", default: false }) : schema;
    })().optional(), strictBindCallApply: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })()]).describe("Check that the arguments for `bind`, `call`, and `apply` methods match the original function.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check that the arguments for `bind`, `call`, and `apply` methods match the original function.", default: false }) : schema;
    })().optional(), strictBuiltinIteratorReturn: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })()]).describe("Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Built-in iterators are instantiated with a `TReturn` type of `undefined` instead of `any`.", default: false }) : schema;
    })().optional(), strictFunctionTypes: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })()]).describe("When assigning functions, check to ensure parameters and the return values are subtype-compatible.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When assigning functions, check to ensure parameters and the return values are subtype-compatible.", default: false }) : schema;
    })().optional(), strictNullChecks: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })()]).describe("When type checking, take into account `null` and `undefined`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "When type checking, take into account `null` and `undefined`.", default: false }) : schema;
    })().optional(), strictPropertyInitialization: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })()]).describe("Check for class properties that are declared but not set in the constructor.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Check for class properties that are declared but not set in the constructor.", default: false }) : schema;
    })().optional(), stripInternal: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })()]).describe("Disable emitting declarations that have `@internal` in their JSDoc comments.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable emitting declarations that have `@internal` in their JSDoc comments." }) : schema;
    })().optional(), suppressExcessPropertyErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })()]).describe("Disable reporting of excess property errors during the creation of object literals.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Disable reporting of excess property errors during the creation of object literals.", default: false }) : schema;
    })().optional(), suppressImplicitAnyIndexErrors: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })()]).describe("Suppress `noImplicitAny` errors when indexing objects that lack index signatures.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Suppress `noImplicitAny` errors when indexing objects that lack index signatures.", default: false }) : schema;
    })().optional(), target: (() => {
      const schema = z.intersection((() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })()]).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })(), z.union([z.enum(["es3", "es5", "es6", "es2015", "es2016", "es2017", "es2018", "es2019", "es2020", "es2021", "es2022", "es2023", "es2024", "es2025", "esnext"]), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "string") {
          const result = z.string().regex(new RegExp("^([Ee][Ss]([356]|(20(1[56789]|2[012345]))|[Nn][Ee][Xx][Tt]))$")).safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])).describe("Set the JavaScript language version for emitted JavaScript and include compatible library declarations.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Set the JavaScript language version for emitted JavaScript and include compatible library declarations.", default: "es5" }) : schema;
    })().optional(), traceResolution: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })()]).describe("Log paths used during the `moduleResolution` process.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Log paths used during the `moduleResolution` process.", default: false }) : schema;
    })().optional(), tsBuildInfoFile: (() => {
      const schema = z.union([(() => {
      const schema = z.string().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })()]).describe("Specify the path to .tsbuildinfo incremental compilation file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the path to .tsbuildinfo incremental compilation file.", default: ".tsbuildinfo" }) : schema;
    })().optional(), typeRoots: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })()]).describe("Specify multiple folders that act like `./node_modules/@types`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify multiple folders that act like `./node_modules/@types`." }) : schema;
    })().optional(), types: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })()]).describe("Specify type package names to be included without being referenced in a source file.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify type package names to be included without being referenced in a source file." }) : schema;
    })().optional(), useDefineForClassFields: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })()]).describe("Emit ECMAScript-standard-compliant class fields.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit ECMAScript-standard-compliant class fields.", default: false }) : schema;
    })().optional(), useUnknownInCatchVariables: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })()]).describe("Default catch clause variables as `unknown` instead of `any`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Default catch clause variables as `unknown` instead of `any`.", default: false }) : schema;
    })().optional(), verbatimModuleSyntax: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })()]).describe("Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the `module` setting." }) : schema;
    })().optional(), watch: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })()]).describe("Watch input files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Watch input files." }) : schema;
    })().optional(), watchDirectory: (() => {
      const schema = z.enum(["useFsEvents", "fixedPollingInterval", "dynamicPriorityPolling", "fixedChunkSizePolling"]).describe("Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching directories under systems that lack recursive file-watching functionality. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional(), watchFile: (() => {
      const schema = z.enum(["fixedPollingInterval", "priorityPollingInterval", "dynamicPriorityPolling", "useFsEvents", "useFsEventsOnParentDirectory", "fixedChunkSizePolling"]).describe("Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify the strategy for watching individual files. Requires TypeScript version 3.8 or later.", default: "useFsEvents" }) : schema;
    })().optional() }).passthrough().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })()]).describe("Instructs the TypeScript compiler how to compile .ts files.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Instructs the TypeScript compiler how to compile .ts files." }) : schema;
    })()).describe("JSON object to merge with TypeScript `compilerOptions`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "JSON object to merge with TypeScript `compilerOptions`." }) : schema;
    })().optional(), emit: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Emit output files into `.ts-node` directory.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit output files into `.ts-node` directory.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Emit output files into `.ts-node` directory.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit output files into `.ts-node` directory.", default: false }) : schema;
    })()]).describe("Emit output files into `.ts-node` directory.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Emit output files into `.ts-node` directory.", default: false }) : schema;
    })().optional(), esm: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules" }) : schema;
    })()]).describe("Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable native ESM support.\n\nFor details, see https://typestrong.org/ts-node/docs/imports#native-ecmascript-modules" }) : schema;
    })().optional(), experimentalReplAwait: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior." }) : schema;
    })()]).describe("Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Allows the usage of top level await in REPL.\n\nUses node's implementation which accomplishes this with an AST syntax transformation.\n\nEnabled by default when tsconfig target is es2018 or above. Set to false to disable.\n\n**Note**: setting to `true` when tsconfig target is too low will throw an Error.  Leave as `undefined`\nto get default, automatic behavior." }) : schema;
    })().optional(), experimentalResolver: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514" }) : schema;
    })()]).describe("Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Enable experimental features that re-map imports and require calls to support:\n`baseUrl`, `paths`, `rootDirs`, `.js` to `.ts` file extension mappings,\n`outDir` to `rootDir` mappings for composite projects and monorepos.\n\nFor details, see https://github.com/TypeStrong/ts-node/issues/1514" }) : schema;
    })().optional(), experimentalSpecifierResolution: (() => {
      const schema = z.enum(["explicit", "node"]).describe("Like node's `--experimental-specifier-resolution`, , but can also be set in your `tsconfig.json` for convenience.\n\nFor details, see https://nodejs.org/dist/latest-v18.x/docs/api/esm.html#customizing-esm-specifier-resolution-algorithm");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Like node's `--experimental-specifier-resolution`, , but can also be set in your `tsconfig.json` for convenience.\n\nFor details, see https://nodejs.org/dist/latest-v18.x/docs/api/esm.html#customizing-esm-specifier-resolution-algorithm" }) : schema;
    })().optional(), files: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.", default: false }) : schema;
    })()]).describe("Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Load \"files\" and \"include\" from `tsconfig.json` on startup.\n\nDefault is to override `tsconfig.json` \"files\" and \"include\" to only include the entrypoint script.", default: false }) : schema;
    })().optional(), ignore: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).describe("Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.", default: ["(?:^|/)node_modules/"] }) : schema;
    })(), (() => {
      const schema = z.null().describe("Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.", default: ["(?:^|/)node_modules/"] }) : schema;
    })()]).describe("Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Paths which should not be compiled.\n\nEach string in the array is converted to a regular expression via `new RegExp()` and tested against source paths prior to compilation.\n\nSource paths are normalized to posix-style separators, relative to the directory containing `tsconfig.json` or to cwd if no `tsconfig.json` is loaded.\n\nDefault is to ignore all node_modules subdirectories.", default: ["(?:^|/)node_modules/"] }) : schema;
    })().optional(), ignoreDiagnostics: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.union([z.string(), z.number()])).describe("Ignore TypeScript warnings by diagnostic code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ignore TypeScript warnings by diagnostic code." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Ignore TypeScript warnings by diagnostic code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ignore TypeScript warnings by diagnostic code." }) : schema;
    })()]).describe("Ignore TypeScript warnings by diagnostic code.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Ignore TypeScript warnings by diagnostic code." }) : schema;
    })().optional(), logError: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Logs TypeScript errors to stderr instead of throwing exceptions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Logs TypeScript errors to stderr instead of throwing exceptions.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Logs TypeScript errors to stderr instead of throwing exceptions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Logs TypeScript errors to stderr instead of throwing exceptions.", default: false }) : schema;
    })()]).describe("Logs TypeScript errors to stderr instead of throwing exceptions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Logs TypeScript errors to stderr instead of throwing exceptions.", default: false }) : schema;
    })().optional(), moduleTypes: z.union([z.object({}).passthrough(), z.null()]).optional(), preferTsExts: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`", default: false }) : schema;
    })()]).describe("Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Re-order file extensions so that TypeScript imports are preferred.\n\nFor example, when both `index.js` and `index.ts` exist, enabling this option causes `require('./index')` to resolve to `index.ts` instead of `index.js`", default: false }) : schema;
    })().optional(), pretty: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use pretty diagnostic formatter.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use pretty diagnostic formatter.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use pretty diagnostic formatter.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use pretty diagnostic formatter.", default: false }) : schema;
    })()]).describe("Use pretty diagnostic formatter.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use pretty diagnostic formatter.", default: false }) : schema;
    })().optional(), require: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).describe("Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results." }) : schema;
    })()]).describe("Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Modules to require, like node's `--require` flag.\n\nIf specified in `tsconfig.json`, the modules will be resolved relative to the `tsconfig.json` file.\n\nIf specified programmatically, each input string should be pre-resolved to an absolute path for\nbest results." }) : schema;
    })().optional(), scope: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Scope compiler to files within `scopeDir`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Scope compiler to files within `scopeDir`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Scope compiler to files within `scopeDir`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Scope compiler to files within `scopeDir`.", default: false }) : schema;
    })()]).describe("Scope compiler to files within `scopeDir`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Scope compiler to files within `scopeDir`.", default: false }) : schema;
    })().optional(), scopeDir: (() => {
      const schema = z.union([(() => {
      const schema = z.string();
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { default: "First of: `tsconfig.json` \"rootDir\" if specified, directory containing `tsconfig.json`, or cwd if no `tsconfig.json` is loaded." }) : schema;
    })(), (() => {
      const schema = z.null();
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { default: "First of: `tsconfig.json` \"rootDir\" if specified, directory containing `tsconfig.json`, or cwd if no `tsconfig.json` is loaded." }) : schema;
    })()]);
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { default: "First of: `tsconfig.json` \"rootDir\" if specified, directory containing `tsconfig.json`, or cwd if no `tsconfig.json` is loaded." }) : schema;
    })().optional(), skipIgnore: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Skip ignore check, so that compilation will be attempted for all files with matching extensions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip ignore check, so that compilation will be attempted for all files with matching extensions.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Skip ignore check, so that compilation will be attempted for all files with matching extensions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip ignore check, so that compilation will be attempted for all files with matching extensions.", default: false }) : schema;
    })()]).describe("Skip ignore check, so that compilation will be attempted for all files with matching extensions.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Skip ignore check, so that compilation will be attempted for all files with matching extensions.", default: false }) : schema;
    })().optional(), swc: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers" }) : schema;
    })(), (() => {
      const schema = z.null().describe("Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers" }) : schema;
    })()]).describe("Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Transpile with swc instead of the TypeScript compiler, and skip typechecking.\n\nEquivalent to setting both `transpileOnly: true` and `transpiler: 'ts-node/transpilers/swc'`\n\nFor complete instructions: https://typestrong.org/ts-node/docs/transpilers" }) : schema;
    })().optional(), transpileOnly: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("Use TypeScript's faster `transpileModule`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's faster `transpileModule`.", default: false }) : schema;
    })(), (() => {
      const schema = z.null().describe("Use TypeScript's faster `transpileModule`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's faster `transpileModule`.", default: false }) : schema;
    })()]).describe("Use TypeScript's faster `transpileModule`.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Use TypeScript's faster `transpileModule`.", default: false }) : schema;
    })().optional(), transpiler: (() => {
      const schema = z.union([z.union([z.array(z.any()).min(2).max(2).superRefine((val, ctx) => {
      const schemas = [z.union([z.string(), z.null()]), z.union([z.object({}).passthrough(), z.null()])];
      for (let i = 0; i < Math.min(val.length, schemas.length); i++) {
        const itemResult = schemas[i].safeParse(val[i]);
        if (!itemResult.success) {
          itemResult.error.issues.forEach(issue => {
            ctx.addIssue({ ...issue, path: [i, ...issue.path] });
          });
        }
      }
    }), z.null()]), z.union([z.string(), z.null()])]).describe("Specify a custom transpiler for use with transpileOnly");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specify a custom transpiler for use with transpileOnly" }) : schema;
    })().optional(), typeCheck: (() => {
      const schema = z.union([(() => {
      const schema = z.boolean().describe("**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).", default: true }) : schema;
    })(), (() => {
      const schema = z.null().describe("**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).", default: true }) : schema;
    })()]).describe("**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "**DEPRECATED** Specify type-check is enabled (e.g. `transpileOnly == false`).", default: true }) : schema;
    })().optional() }).passthrough().describe("ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support." }) : schema;
    })(), (() => {
      const schema = z.null().describe("ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support." }) : schema;
    })()]).describe("ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "ts-node options.  See also: https://typestrong.org/ts-node/docs/configuration\n\nts-node offers TypeScript execution and REPL for node.js, with source map support." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })), z.union([z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ files: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included." }) : schema;
    })(), (() => {
      const schema = z.null().describe("If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included." }) : schema;
    })()]).describe("If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. When a 'files' property is specified, only those files and those specified by 'include' are included." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ exclude: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later." }) : schema;
    })()]).describe("Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of files to be excluded from compilation. The 'exclude' property only affects the files included via the 'include' property and not the 'files' property. Glob patterns require TypeScript version 2.0 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ include: (() => {
      const schema = z.union([(() => {
      const schema = z.array(z.string()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later." }) : schema;
    })(), (() => {
      const schema = z.null().describe("Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later." }) : schema;
    })()]).describe("Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Specifies a list of glob patterns that match files to be included in compilation. If no 'files' or 'include' property is present in a tsconfig.json, the compiler defaults to including all files in the containing directory and subdirectories except those specified by 'exclude'. Requires TypeScript version 2.0 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      }), z.unknown().superRefine((val, ctx) => {
        if (typeof val === "object" && val !== null && !Array.isArray(val)) {
          const result = z.object({ references: (() => {
      const schema = z.array((() => {
      const schema = z.object({ path: (() => {
      const schema = z.string().describe("Path to referenced tsconfig or to folder containing tsconfig.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Path to referenced tsconfig or to folder containing tsconfig." }) : schema;
    })().optional() }).passthrough().describe("Project reference.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Project reference." }) : schema;
    })()).refine((arr) => {
      const seen = new Set();
      for (const item of arr) {
        const key = JSON.stringify(item);
        if (seen.has(key)) return false;
        seen.add(key);
      }
      return true;
    }, { message: "Array items must be unique" }).describe("Referenced projects. Requires TypeScript version 3.0 or later.");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Referenced projects. Requires TypeScript version 3.0 or later." }) : schema;
    })().optional() }).passthrough().safeParse(val);
          if (!result.success) {
            result.error.issues.forEach(issue => ctx.addIssue(issue));
          }
        }
      })])));
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { title: "JSON schema for the TypeScript compiler's configuration file" }) : schema;
    })();
type another_TSConfigNativeType = z.infer<typeof another_TSConfigNative>;

const another_Native = z.object({ fruits: z.array(z.enum(["apple", "orange", "pear"])), vegetables: z.array(z.object({ veggieLike: z.boolean(), veggieName: z.string() }).passthrough()) }).passthrough();
type another_NativeType = z.infer<typeof another_Native>;

const user_Calendar = (() => {
      const schema = z.object({ category: z.string().optional(), description: z.string().optional(), duration: (() => {
      const schema = z.string().describe("Event duration");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Event duration" }) : schema;
    })().optional(), endDate: (() => {
      const schema = z.string().describe("Event ending time");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Event ending time" }) : schema;
    })().optional(), geo: (() => {
      const schema = z.object({ lat: z.number().optional(), lng: z.number().optional() }).passthrough().describe("Geographical location");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Geographical location" }) : schema;
    })().optional(), location: z.string().optional(), recurrenceDate: (() => {
      const schema = z.string().describe("Recurrence date");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Recurrence date" }) : schema;
    })().optional(), recurrenceDule: (() => {
      const schema = z.string().describe("Recurrence rule");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Recurrence rule" }) : schema;
    })().optional(), startDate: (() => {
      const schema = z.string().describe("Event starting time");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "Event starting time" }) : schema;
    })().optional(), summary: z.string(), url: z.string().optional(), dtstart: z.any() }).passthrough().superRefine((val, ctx) => {
      for (const key of ["dtstart"]) {
        if (!Object.hasOwn(val, key)) {
          ctx.addIssue({ code: z.ZodIssueCode.custom, path: [key], message: "Required" });
        }
      }
    }).describe("A representation of an event");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "A representation of an event" }) : schema;
    })();
type user_CalendarType = z.infer<typeof user_Calendar>;

const user_User = (() => {
      const schema = z.object({ age: z.number().int().gte(0).optional(), email: z.string().email(), id: z.string(), name: z.string().optional() }).passthrough().describe("A user object");
      const meta = Reflect.get(schema, "meta");
      return typeof meta === "function" ? meta.call(schema, { description: "A user object" }) : schema;
    })();
type user_UserType = z.infer<typeof user_User>;

export const schemas = {
  "another:TSConfig": another_TSConfig,
  "another:TSConfigNative": another_TSConfigNative,
  "another:Native": another_Native,
  "user:Calendar": user_Calendar,
  "user:User": user_User,
} as const;

export type SchemaTypes = {
  "another:TSConfig": another_TSConfigType;
  "another:TSConfigNative": another_TSConfigNativeType;
  "another:Native": another_NativeType;
  "user:Calendar": user_CalendarType;
  "user:User": user_UserType;
};

declare module '@vectorfyco/valbridge' {
  interface Register {
    schemas: typeof schemas;
    schemaTypes: SchemaTypes;
  }
}
